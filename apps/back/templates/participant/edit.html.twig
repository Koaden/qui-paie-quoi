{{ form_start(form, { 'action': path('participant.edit', {groupId: groupId, id: id}) }) }}
    {{ component('Field', { field: form.name }) }}
    {{ component('Button', {
        text: 'action.edit'|trans,
        submit: true,
        size: 'large'
    }) }}
{{ form_end(form) }}
