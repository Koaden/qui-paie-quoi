{{ form_start(form, { 'action': path('participant.add', {groupId: id}) }) }}
    {{ component('Field', { field: form.name }) }}
    {{ component('Button', {
        text: 'action.add'|trans,
        submit: true,
        size: 'large'
    }) }}
{{ form_end(form) }}
